package main

// version is stamped from the repository VERSION and commit hash by every
// GUI build path. The fallback keeps an ad-hoc `go build` honest about being a
// development binary instead of borrowing the last release's identity.
var version = "0.0.0-dev"
