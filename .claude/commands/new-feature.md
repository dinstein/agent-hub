---
description: Build a feature by the repo's standard flow — worktree, one commit per subtask, land fast-forward only
argument-hint: [what to build]
---

Read `runbooks/new-feature.md` and follow it from step 0, for: $ARGUMENTS

This wrapper carries no procedure of its own. If a step here would differ from the runbook, the
runbook wins — edit that file instead.
